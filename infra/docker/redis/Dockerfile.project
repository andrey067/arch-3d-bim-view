# Redis Dockerfile — uses project root as build context
FROM redis:7-alpine

# AOF enabled for durability; noeviction so queue jobs aren't lost
COPY infra/docker/redis/redis.conf /usr/local/etc/redis/redis.conf

EXPOSE 6379

CMD ["redis-server", "/usr/local/etc/redis/redis.conf"]
