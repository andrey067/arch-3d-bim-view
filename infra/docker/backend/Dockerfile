# This Dockerfile has moved to app/backend/Dockerfile
# The canonical Dockerfile is now in the application directory.
#
# See: app/backend/Dockerfile
# Build from project root: docker build -f app/backend/Dockerfile .
