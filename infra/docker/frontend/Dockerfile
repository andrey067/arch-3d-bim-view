# This Dockerfile has moved to app/frontend/Dockerfile
# The canonical Dockerfile is now in the application directory.
#
# See: app/frontend/Dockerfile
# Build from project root: docker build -f app/frontend/Dockerfile app/frontend
