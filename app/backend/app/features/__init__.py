"""Vertical-slice features.

Each feature ships its own `router.py`, `schemas.py`, `service.py`,
`persistence.py`, `models.py`, and `tests/`. Sprint 0 only declares
the package; individual features arrive from Sprint 1 onward.
"""
